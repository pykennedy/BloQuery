<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:paddingLeft="6dp"
    android:paddingRight="6dp"
    android:paddingTop="2dp"
    android:paddingBottom="6dp"
    android:layout_margin="2dp"
    android:background="@drawable/question_ripple">

    <TextView
        android:id="@+id/full_qa_question_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:textSize="20sp"
        android:textColor="@color/text_primary"/>

    <TextView
        android:id="@+id/full_qa_question_user"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/full_qa_question_text"
        android:clickable="true"
        android:background="?attr/selectableItemBackground"
        android:textSize="14sp"
        android:textColor="@color/text_secondary_color"/>

    <TextView
        android:id="@+id/full_qa_question_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/full_qa_question_user"
        android:textSize="12sp"
        android:textColor="@color/text_hint"/>

</RelativeLayout>